package com.gincosapps.complexer;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {

    /*
    @Test
    public void addition_isCorrect() throws Exception {
        assertEquals(4, 2 + 2);
    }
    
    @Test
    public void sum_isCorrect() throws Exception{
        ComplexNumber a = new ComplexNumber(1,1);
        ComplexNumber b = new ComplexNumber(2,2);
        ComplexNumber c = new ComplexNumber(3,3);
        double totalX = ComplexNumber.sum(a,b,c).getX();
        double totalY = ComplexNumber.sum(a,b,c).getY();
        
        assertEquals(6, totalX, 0.0001);
        assertEquals(6, totalY, 0.0001);
    }
    */
}